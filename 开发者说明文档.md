# 工作汇报输出器开发者说明文档

本文档说明 Flutter 版本的本地开发、调试、测试和打包流程。旧版 Python/Tkinter 文件仍保留在项目中，当前跨端版本的主要代码在 `lib/`、`test/`、`scripts/` 和 `.github/workflows/`。

## 1. 开发环境

当前机器已安装并配置：

- Flutter SDK：`C:\Users\chanw\development\flutter`
- Android SDK：`C:\Users\chanw\AppData\Local\Android\Sdk`
- JDK 17：`C:\Program Files\Eclipse Adoptium\jdk-17.0.19.10-hotspot`
- Pub 镜像：`https://pub.flutter-io.cn`
- Flutter 存储镜像：`https://storage.flutter-io.cn`

如果刚安装完环境，建议重开 PowerShell 或 VS Code 终端，让 PATH 生效。

验证环境：

```powershell
flutter doctor -v
```

期望结果是 `No issues found!`。如果提示 Android license 问题，执行：

```powershell
flutter doctor --android-licenses
```

## 2. 项目结构

主要目录和文件：

- `lib/main.dart`：Flutter 应用入口和主界面。
- `lib/models/`：数据模型。
- `lib/services/`：汇报生成、存储、AI、企业微信等业务服务。
- `test/`：单元测试和基础 Widget 测试。
- `scripts/package.ps1`：Windows PowerShell 本地打包脚本。
- `scripts/package.sh`：macOS/Linux shell 本地打包脚本。
- `.github/workflows/flutter-release.yml`：GitHub Actions 三端构建流程。
- `pubspec.yaml`：Flutter 依赖、版本号和资源配置。
- `pubspec.lock`：依赖锁定文件，建议提交。

## 3. 首次准备

进入项目目录：

```powershell
cd "C:\Users\chanw\Documents\常用脚本\工作汇报输出器"
```

安装依赖：

```powershell
flutter pub get
```

如果当前平台工程目录不存在，例如 `windows/`、`android/`、`macos/`，可以生成当前需要的平台工程：

```powershell
flutter create --project-name work_report_generator --platforms=windows .
flutter create --project-name work_report_generator --platforms=android .
```

在 macOS 上生成 macOS 平台工程：

```bash
flutter create --project-name work_report_generator --platforms=macos .
```

## 4. 本地运行和调试

Windows 桌面运行前，需要开启 Windows “开发人员模式”，否则 Flutter 使用插件时会报错：

```text
Error: Building with plugins requires symlink support.
```

打开设置页：

```powershell
start ms-settings:developers
```

在设置页中开启“开发人员模式”后，关闭当前终端并重新打开，再继续运行。

Windows 桌面运行：

```powershell
flutter run -d windows
```

Chrome Web 预览：

```powershell
flutter run -d chrome
```

Web 预览主要用于快速检查界面和基础流程。Web 端数据会保存到浏览器 `localStorage`，和 Windows/macOS/Android 的本地应用文档目录不是同一个存储位置。

Android 设备运行：

```powershell
flutter devices
flutter run -d <设备ID>
```

Android 本地调试需要一个可识别的 Android 设备，二选一即可：

- 真机：打开手机“开发者选项”和“USB 调试”，连接电脑后运行 `flutter devices`，看到设备后执行 `flutter run -d <设备ID>`。
- 模拟器：安装 Android Studio，进入 `Device Manager` 创建并启动一个 Android Virtual Device，然后再运行 `flutter devices`。

当前项目所在路径包含中文目录。Windows 上 Android SDK 的部分工具对中文路径兼容不好，Android 真机调试建议从英文 junction 路径进入项目：

```powershell
cd "C:\Users\chanw\work_report_generator_android"
flutter run -d <设备ID>
```

这个路径是指向原项目目录的 junction，不是复制项目。原项目仍在：

```powershell
C:\Users\chanw\Documents\常用脚本\工作汇报输出器
```

如果需要一边操作手机一边收集完整报错日志，使用 Android 调试脚本：

```powershell
cd "C:\Users\chanw\work_report_generator_android"
.\scripts\android_debug.ps1 -DeviceId AGMK6R5C19001623
```

脚本会同时保存：

- `logs\flutter_run_时间.log`：`flutter run --verbose` 输出，Dart 红屏堆栈优先看这个。
- `logs\adb_logcat_时间.log`：Android 系统日志，用于定位原生、安装、权限、厂商系统相关问题。

运行脚本后在手机上复现问题，复现后回到终端按 `q` 退出，再查看最新的 `logs` 文件。

常用调试操作：

- 终端中按 `r`：热重载。
- 终端中按 `R`：热重启。
- 终端中按 `q`：退出运行。
- VS Code 可直接打开 `lib/main.dart`，选择设备后按 `F5` 调试。

## 5. 质量检查

每次提交前建议运行：

```powershell
flutter analyze
flutter test
```

当前已验证通过：

- `flutter analyze`
- `flutter test`

如果依赖变更，先运行：

```powershell
flutter pub get
```

## 6. 本地打包

本地打包脚本会自动：

1. 生成对应平台工程。
2. 补 Android 网络权限或 macOS 网络 entitlement。
3. 执行 `flutter pub get`。
4. 执行 `flutter analyze`。
5. 执行 `flutter test`。
6. 构建目标平台产物。

Windows 打包：

```powershell
.\scripts\package.ps1 -Target windows
```

产物位置通常包括：

- `build\windows\x64\runner\Release\`
- MSIX 产物由 `msix` 插件生成。

Android APK 打包：

```powershell
.\scripts\package.ps1 -Target android
```

产物位置：

```text
build\app\outputs\flutter-apk\app-release.apk
```

在 macOS 上打包 macOS 应用：

```bash
bash scripts/package.sh macos
```

在 macOS/Linux 上打包 Android：

```bash
bash scripts/package.sh android
```

`all` 目标说明：

```powershell
.\scripts\package.ps1 -Target all
```

在 Windows 上会构建 Windows 和 Android；macOS 安装包必须在 macOS 环境中构建。

## 7. CI 一键打包

GitHub Actions 工作流文件：

```text
.github/workflows/flutter-release.yml
```

触发方式：

1. 打开 GitHub 仓库。
2. 进入 `Actions`。
3. 选择 `Flutter Release`。
4. 点击 `Run workflow`。

CI 会执行：

- Ubuntu：`flutter pub get`、`flutter analyze`、`flutter test`
- Android：生成 `app-release.apk`
- Windows：生成 Windows Release 目录和 MSIX
- macOS：生成 DMG

产物在对应 workflow run 的 `Artifacts` 中下载。

## 8. 数据和兼容

Flutter 版本的数据保存到系统应用文档目录下的：

```text
工作汇报记录
```

首次启动时，如果当前运行目录存在旧版 `工作汇报记录`，且新数据目录为空，应用会尝试导入旧数据。

企业微信能力采用跨端降级策略：

- 所有平台保留复制汇报内容。
- Windows 尝试打开企业微信。
- macOS 和 Android 不依赖 Windows 注册表或 `WXWork.exe`，复制后由用户手动粘贴。

## 9. 常见问题

如果提示找不到 `flutter`：

1. 关闭当前终端。
2. 重新打开 PowerShell 或 VS Code。
3. 再执行：

```powershell
flutter --version
```

如果下载依赖很慢，确认镜像环境变量：

```powershell
$env:PUB_HOSTED_URL
$env:FLUTTER_STORAGE_BASE_URL
```

如果 Android 构建失败，先确认：

```powershell
flutter doctor -v
flutter devices
```

如果 `flutter devices` 看不到 Android 设备，说明当前没有连接真机，也没有启动模拟器。先启动 Android Studio 的模拟器，或连接已开启 USB 调试的 Android 真机。

如果 Gradle 下载时报 `Connection reset`，通常是 Gradle 官方下载链路不稳定。项目已将 Gradle Wrapper 指向国内镜像，并给 Android 仓库添加了镜像 fallback。

如果报 `NDK ... did not have a source.properties file`，说明 NDK 下载不完整，可以删除提示中的坏目录后重新安装，例如：

```powershell
Remove-Item -LiteralPath "C:\Users\chanw\AppData\Local\Android\Sdk\ndk\28.2.13676358" -Recurse -Force
sdkmanager "ndk;28.2.13676358"
```

如果报 `Illegal byte sequence` 或 `AndroidManifest.xml not found`，且 APK 路径里有中文目录，请从英文 junction 路径运行：

```powershell
cd "C:\Users\chanw\work_report_generator_android"
flutter run -d <设备ID>
```

如果手机安装时提示 `INSTALL_FAILED_ABORTED: User rejected permissions`，说明手机端安装确认被拒绝或超时。重新运行命令，并在手机上允许 USB 调试和安装确认。

如果 Windows 构建失败，确认 Visual Studio 桌面开发组件存在：

```powershell
flutter doctor -v
```

如果 Windows 运行或打包时报 `Building with plugins requires symlink support`，开启“开发人员模式”：

```powershell
start ms-settings:developers
```

如果 Chrome Web 运行时报 `MissingPluginException`，通常是某个插件没有 Web 实现，或代码在 Web 端调用了仅桌面/移动端可用的 API。当前项目的存储和企业微信服务已做平台分流；修改相关服务时要继续使用条件导出，避免 Web 端直接调用 `path_provider`、Windows 注册表、进程查询等平台 API。

## 10. 推荐开发流程

日常开发建议：

```powershell
cd "C:\Users\chanw\Documents\常用脚本\工作汇报输出器"
flutter pub get
flutter run -d windows
```

提交前检查：

```powershell
flutter analyze
flutter test
```

发布前打包：

```powershell
.\scripts\package.ps1 -Target windows
.\scripts\package.ps1 -Target android
```

三端正式产物建议通过 GitHub Actions 的 `Flutter Release` 工作流统一生成。
