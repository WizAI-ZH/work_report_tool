# 更新日志

本文档记录"威智工作汇报器"的版本更新内容。发布脚本会读取对应版本的小节，并同步写入 GitHub Release 描述。

## [1.2.24] - 2026-08-10

### 修复

- 修复 CI Release 未上传 NSIS .exe 安装器的问题：setup.exe 在 artifact 中位于 `dist/` 子目录，release job 原路径检查遗漏，改用 `find` 动态查找。

## [1.2.23] - 2026-08-10

### 新增

- Android APK 按 ABI 拆分（arm64-v8a / armeabi-v7a / x86_64），单包体积从 ~53MB 降至 ~20MB。应用内更新自动匹配当前设备 CPU 架构，下载对应 split APK。
- Windows 端 NSIS .exe 安装器：CI 自动用 makensis 打包散文件为 .exe 安装器，支持 `/S` 静默安装。应用内更新下载 .exe 后自动启动安装器，安装器会自动关闭旧进程、替换文件、重启应用，无需手动操作。

### 修复

- 修复 Android split APK 文件名匹配：文件名格式为 `WorkReportGenerator_android_<abi>_<version>.apk`（版本号在 ABI 后），原 `endsWith` 匹配不到，改用 `contains` 匹配 ABI 片段。

## [1.2.22] - 2026-08-06

### 修复

- 修复应用内更新功能报错"发现新版本，但没有找到当前平台可用的安装包"：`UpdateService._preferredAsset` 原用 `endsWith('_windows.zip')` 匹配，但实际文件名格式为 `WorkReportGenerator_windows_1.2.21.zip`（版本号在中间），导致匹配失败。改用 `contains` + `endsWith` 组合匹配，同时 Windows 新增 `.msix` 格式支持。
- CI Release 产物 Windows zip 命名改为 `WorkReportGenerator_<版本>_windows.zip`，兼容旧版本应用的 `endsWith('_windows.zip')` 匹配逻辑，确保旧版本用户也能正常自动更新。

## [1.2.21] - 2026-08-06

### 新增

- Android 端无障碍服务全面重写：基于 uiautomator 真实控件树实现企微自动发送，支持搜索入口（id=nsi）定位、搜索框（id=lnx）填入群名、聊天页消息输入框识别、发送按钮多语言匹配（傳送/發送/发送/Send）。
- 新增 `ensureWeWorkForeground()`：通过 `getLaunchIntentForPackage` 拉起企微前台，避免直接指定未 exported 的 Activity 导致 Permission Denial。
- 新增 `ensureHomePage()`：按 Back 键逐级退出子界面，验证主界面特征（搜索入口 id=nsi、底部 tab 訊息/消息）后才执行搜索，确保搜索在全局面非当前群内进行。
- 黑屏过渡页容错：连续 root null 检测，连续 2 次时重新拉起企微跳出黑屏，最多尝试 10 次。
- 群名繁简宽松匹配：扩展 200+ 常见字繁简映射表，用户设简体群名可匹配企微繁体界面。
- Windows 端 `send_to_wework.py` 独立 exe 打包（PyInstaller），用户机器无需安装 Python；含 OCR 验证会话标题、DPI 缩放统一物理像素坐标、冷启动 hwnd 稳定性检测。

### 修复

- 修复模板填写区右侧溢出 14px 警告（RIGHT OVERFLOWED BY 14 PIXELS）：标题行 Row 改为 Wrap 自动换行，模板内 TextField 行用 LayoutBuilder 根据宽度切换横竖排。
- 修复改群名时红屏报错（TextEditingController disposed）：将企微设置对话框抽为独立 StatefulWidget `_WechatTargetDialog`，由其管理 controller 生命周期。
- 修复发送按钮未找到（no_send_button）：step6 改为轮询 6 秒查找，支持繁简英四种发送按钮文字。
- 修复企微退回主界面时黑屏过渡页卡死：连续 root null 检测 + 重新拉起企微机制。

## [1.2.9] - 2026-08-05

### 修复

- 修复“发送到企微”在 Windows 上完全不生效的问题：之前版本未启动企微进程即尝试查找窗口，导致无任何反应。
- Windows 端改用 Python（pyautogui + pygetwindow）实现全自动发送：pygetwindow 强制激活企微窗口并弹出前台，pyautogui 用 SendInput 完成 Ctrl+F 搜索、粘贴群名、进入会话、粘贴汇报、回车发送全流程。
- Python 脚本不可用时回退为：启动企微 + 复制到剪贴板，提示手动粘贴。

## [1.2.8] - 2026-08-05

### 新增

- “发送到企微”改为全自动一键发送：点击后自动打开企业微信、搜索目标群、进入会话、粘贴汇报内容并回车发送，用户无需手动操作。
- Windows 端用 .NET UIAutomation 查找企微主窗口并前台激活，配合键盘模拟完成搜索、粘贴、发送全流程。
- Android 端新增无障碍服务（AccessibilityService），首次使用时引导用户在系统设置中开启权限，开启后可自动操作企微控件树完成发送。

## [1.2.7] - 2026-08-05

### 新增

- 新增“企微”设置入口，可配置“发送到企微”按钮自动搜索的群名，默认“文件传输助手”。
- “发送到企微”按钮在 Windows 下会自动打开企业微信并按群名搜索进入对应会话，汇报内容已复制到剪贴板，直接 Ctrl+V 粘贴即可。
- 工作内容填写区新增“模板填写”模式：把每个大区拆成多行，每行固定“任务名 / 进度 / 当前工作节点 / 预期工作”四列，支持新建行、删除行，Tab 键在列间快速切换；空列不会生成对应括号内容，例如已完成的任务空掉“预期工作”会生成“任务名（进度，当前节点）”。

### 优化

- 优化 AI 建议提示词，针对历史记录中反复出现的稳定性问题强化约束：今日工作进度严禁带“预计”、条目末尾禁止省略号、禁止用“无”作为编号占位、明日计划进度统一写“预计XX%”。
- AI 建议弹窗的“重新生成建议”栏下方新增常用调整建议快捷按钮（如“明日工作计划需要补充预计进度”“今日工作进度不要写‘预计’”等），点击即可填入。

## [1.2.6] - 2026-06-16

### 优化

- 历史记录改为先打开预览详情，不再点击条目后直接覆盖当前编辑内容。
- 历史记录预览支持复制汇报内容，只有点击“应用到当前编辑”才会恢复到编辑框。

## [1.2.5] - 2026-06-04

### 优化

- AI 建议提示词新增标准参考样例，强化今日工作和明日计划的输出风格。
- 明确 AI 只能学习样例格式和筛选方式，不能复制样例任务到最终结果。
- 补充样例说明，指导 AI 识别 100% 完成事项、未完成事项和由他人继续处理的事项。

## [1.2.4] - 2026-05-26

### 优化

- 优化 AI 建议对明日工作计划的生成规则：未完成的项目性工作会默认延续到明日计划。
- 日常性、持续跟进类工作会保留到明日计划，但不强制填写预计进度。
- 强化项目性工作格式要求，按“工作内容、进度、当前完成简述、预计后续完成内容”整理今日工作。

## [1.2.3] - 2026-05-22

### 优化

- 优化 AI 建议提示词，强化固定汇报格式、事实约束和明日计划推导规则。
- AI 建议改为更低随机性，减少无关任务、错误进度和额外说明。
- 自动清理 AI 返回中的 Markdown 代码块、前置说明等无关文本，尽量只保留可直接应用的汇报正文。

## [1.2.2] - 2026-05-21

### 修复

- 修复跨日期自动刷新后“明日工作计划”会被任务追踪内容自动填充的问题。
- 新日期启动时只将上一日“明日工作计划”带入“今日工作完成情况”，新的“明日工作计划”保持为空，等待用户手动填写。

## [1.2.1] - 2026-05-20

### 修复

- 恢复旧 Python 版的跨日期自动刷新：新日期启动时会把上一日“明日工作计划”带入今日工作内容。
- 自动刷新只在草稿日期早于当天时触发，避免同一天重复打开时覆盖用户正在编辑的内容。

## [1.2.0] - 2026-05-19

### 新增

- 新增“导入与同步”入口，支持导入已生成汇报、历史 JSON 和同步文件。
- 新增 `.wizsync.json` 同步文件，可在电脑和手机之间同步当前草稿和历史记录。
- 新增当前草稿二维码同步，可用手机扫码快速导入电脑端正在编辑的内容。
- Android 新增扫码导入能力，并使用 unbundled MLKit 降低安装包体积增长。

## [1.1.12] - 2026-05-18

### 修复

- 优化 Android AI 建议弹窗的键盘体验，点击空白处可收起键盘。
- 点击“重新生成”前会自动收起键盘，避免新建议内容被输入法遮挡。
- AI 建议内容区域会随键盘高度调整并保持可滚动。

## [1.1.11] - 2026-05-18

### 说明

- 测试自动更新用小版本，用于验证 `1.1.10` 的 Android ABI 分包自动选择和下载流程。
- 无业务功能变更。

## [1.1.10] - 2026-05-18

### 优化

- Android 窄屏顶部栏改为自适应布局，保留完整标题和版本号。
- Android 窄屏顶部栏保留“更新”和“更多”图标按钮，将“AI”“模板”和“历史”收进更多菜单。
- Android Release 增加 `arm64-v8a`、`armeabi-v7a`、`x86_64` 分包 APK。
- Android 自动更新会读取设备支持的 CPU 架构，优先下载对应 ABI APK。
- 继续保留通用 Android APK，兼容旧客户端和手动下载。
- 压缩应用 Logo 资源，降低安装包内 asset 体积。

## [1.1.9] - 2026-05-18

### 说明

- 测试自动更新用小版本，用于验证 `1.1.8` 的静默安装、安装完成后自动启动和安装完成页选项。
- 无业务功能变更。

## [1.1.8] - 2026-05-18

### 优化

- Windows 自动更新下载完成后以静默参数启动安装器，减少安装向导点击步骤。
- Windows 安装器静默更新完成后自动重新启动软件。
- Windows 安装器普通安装完成页新增“启动软件”和“查看 README”选项。
- Windows 安装包内随附 `README.md`，便于安装后查看说明。

### 说明

- Windows UAC 权限确认仍属于系统安全边界，无法由普通应用绕过。

## [1.1.7] - 2026-05-18

### 说明

- 测试自动更新用小版本，用于验证 `1.1.6` 的下载进度弹窗、下载完成提示和安装器启动流程。
- 无业务功能变更。

## [1.1.6] - 2026-05-18

### 优化

- 自动更新下载过程中显示进度弹窗，包含下载阶段、文件名、已下载大小和百分比。
- 下载完成后显示正在启动安装流程，避免用户误以为程序卡死。
- GitHub Release 不再上传 Windows zip 资产，避免旧客户端误选压缩包作为更新安装包。

## [1.1.5] - 2026-05-18

### 修复

- 修复旧客户端在 Windows 上自动启动 NSIS 安装器时报“请求的操作需要提升”的问题。
- Windows NSIS 安装器改为普通进程可启动，再由安装器自身弹出 UAC 提权确认。

### 说明

- 为兼容已安装的旧客户端，本版本 Release 不提供 Windows zip 资产，确保旧客户端优先下载 `windows_setup.exe`。

## [1.1.4] - 2026-05-18

### 修复

- 修复 Windows 自动更新可能选中 `.zip` 压缩包并当作程序启动的问题。
- Windows 更新资产选择改为优先使用 `_windows_setup.exe` 安装器。
- 如果 Windows 只下载到压缩包，则打开文件所在位置，不再尝试直接执行压缩包。

### 说明

- 为兼容已安装的 `1.1.2` 和 `1.1.3` 旧客户端，本版本 Release 不提供 Windows zip 资产，避免旧客户端误选压缩包。

## [1.1.3] - 2026-05-18

### 说明

- 测试自动更新用小版本，用于验证 `1.1.2` 能否检测、下载并启动安装本版本。
- 无业务功能变更。

## [1.1.2] - 2026-05-18

### 修复

- 修复 Windows 端点击更新后只打开 GitHub Release 页面的问题，改为自动下载 NSIS 安装包并启动安装器。
- 修复 macOS 端更新逻辑，检测到 `.dmg` 或 `.pkg` 后自动下载并打开安装包。
- 修复应用内显示版本号未随构建版本同步的问题。

### 说明

- Android、Windows、macOS 均会尽量自动下载安装包并拉起系统安装流程。
- 受系统安全限制，Android 安装页、Windows UAC 和安装器确认仍需用户手动确认。

## [1.1.1] - 2026-05-15
### 说明
- 测试自动更新用小版本

## [1.1.0] - 2026-05-15

### 新增

- 新增启动时自动检查 GitHub Release 最新版本的能力。
- 新增右上角“更新”按钮，可手动检查新版本。
- Android 端发现新版本后可下载 Release APK，并拉起系统安装确认页完成更新。
- Windows/macOS 端发现新版本后会打开 GitHub Release 下载页。

### 说明

- Android 系统不允许普通应用静默安装更新；点击更新后仍需要用户在系统安装页手动确认。
- Android 如未授予“安装未知应用”权限，会先打开权限设置页，允许后可回到应用重新点击更新。

## [1.0.0] - 2026-05-14

### 新增

- 从 Python/Tkinter 桌面程序迁移为 Flutter 跨平台应用。
- 支持 Windows、Android 和 macOS 项目结构。
- 新增 Material 3 中文界面，支持桌面和移动端自适应布局。
- 新增汇报生成、汇报复制、历史记录、模板配置和任务追踪能力。
- 新增 AI 配置、模型获取、连接测试、AI 建议生成和不满意原因重新生成功能。
- 首次启动时引导用户填写 API Key，并展示免费 API Key 获取地址。
- 新增中文日期选择体验。
- 新增应用内版本号显示。
- 新增“威智工作汇报器”应用名称和 `wiz_work_repo_tool_logo.png` 应用图标。
- 新增 Android APK、Windows MSIX/压缩包、macOS 构建脚本和 GitHub Actions 构建工作流。
- Windows MSIX Release 附带 `.cer` 证书和一键证书安装脚本，便于自签名包首次安装。
- 新增 Windows NSIS `.exe` 安装包，作为普通用户推荐下载项。
- Windows NSIS 安装包启动时支持选择安装语言，默认使用简体中文。

### 修复

- 修复 Android 上 API Key 输入框使用安全键盘导致难以粘贴的问题。
- 修复 API Key 显示、隐藏、粘贴、清空按钮被输入框抢点击的问题。
- 修复 AI 配置弹窗内提示信息被弹窗遮挡的问题。
- 修复历史任务去重和明日工作计划重复追加的问题。
- 修复 Android 上 Flutter `_dependents.isEmpty` 断言崩溃。
- 修复软件右上角图标按钮在移动端不易点击的问题。
- 修复 Windows MSIX 使用未随包分发的测试证书导致安装时报 `0x800B010A` 的问题。
- Windows 安装分发增加无需预安装证书的 NSIS 安装包，降低 MSIX 自签名证书带来的安装门槛。

### 调整

- 将“发送到企微”和“AI 建议”调整为主操作区优先按钮。
- 移除多余的“生成并复制”按钮。
- 企业微信能力改为跨平台降级策略：优先复制内容，Windows 尝试打开企业微信，其他平台提示手动粘贴。
- Windows NSIS 安装包默认安装目录调整为 `C:\威智工作汇报器`。
- 废弃 PyInstaller 作为主构建入口，旧 Python 文件仅保留作迁移参考。
